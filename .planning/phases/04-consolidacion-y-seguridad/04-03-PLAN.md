---
phase: 4
plan: 3
subsystem: admin-ui
depends_on: [04-01-PLAN.md, 04-02-PLAN.md]
files_modified:
  - "src/app/api/docs/route.ts"
  - "src/components/admin/AdminPage.tsx"
autonomous: true
requirements: [UX-01]
---

# Phase 4 Plan 3: Dynamic Document Fetch

<objective>
Replace the hardcoded active documents list in `/admin` with a dynamic fetch from an n8n endpoint.
</objective>

<tasks>
1. **Create Docs List API Route:**
   `type="auto"`
   Create `src/app/api/docs/route.ts` that issues a GET request to a new n8n webhook (or mock it using an internal array state on the server for the demo if n8n endpoint isn't ready. For this demo, let's store docs in a server-side JSON file or mock array temporarily, as n8n doesn't have a "list documents" webhook setup yet. Wait, we can mock it purely on the server: `process.cwd() + '/data/docs.json'`. Write to it in the ingest route, read from it here).

2. **Update AdminPage to Fetch Docs:**
   `type="auto"`
   Update `AdminPage.tsx` to load the list of documents dynamically via `/api/docs` in a `useEffect` on mount.
</tasks>

<verification>
1. Refreshing `/admin` does not reset the documents back to the initial 3 hardcoded files; it shows the real list of uploaded files (stored in data/docs.json).
</verification>
