---
phase: 7
plan: 2
subsystem: directory-navigation
depends_on: [07-01-PLAN]
files_modified:
  - "src/components/chat-interface.tsx"
  - "src/app/admin/page.tsx"
autonomous: true
requirements_completed: [NAV-03]
---
# Phase 7 Plan 2: Navigation Back to Directory

## Objective
Ensure users can easily navigate back to the root directory `/` from the chat and admin interfaces.

## Steps
1. Add a "Home" or "Directory" icon/link in the Header block of `src/components/chat-interface.tsx` pointing to `/`.
2. Add a similar navigational element pointing to `/` in the header or `Button variant="ghost"` on `src/app/admin/page.tsx`.

## Verification
- User is on `/[tenant]` chat room and clicks the Home icon, taking them successfully to `/`.
- Admin clicks "Back to Directory" from their dashboard and returns safely to `/`.
