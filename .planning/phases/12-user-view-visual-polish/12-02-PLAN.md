---
phase: 12-user-view-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/page.tsx
  - src/components/admin/AdminPage.tsx
autonomous: true
requirements: [VIS-01, VIS-06]

must_haves:
  truths:
    - "Directory page shows 6 tenant cards in a 2x3 grid (2 rows, 3 columns on desktop)"
    - "Each directory card displays the tenant's brand logo image"
    - "Long tenant names (e.g. 'SegurCaixa Adeslas') do not wrap to 2 lines"
    - "Admin header shows the active tenant's logo beside the title"
    - "Card design remains identical to current style — no redesign"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Directory page with 2x3 branded grid"
      contains: "logoUrl"
    - path: "src/components/admin/AdminPage.tsx"
      provides: "Admin header with tenant logo"
      contains: "logoUrl"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/tenants.ts"
      via: "TENANTS import and logoUrl rendering"
      pattern: "tenant\\.theme\\.logoUrl"
    - from: "src/components/admin/AdminPage.tsx"
      to: "src/lib/tenants.ts"
      via: "tenant prop with theme.logoUrl"
      pattern: "logoUrl"
---

<objective>
Update the directory page to display 6 tenant cards in a 2x3 branded grid with logos, and add tenant logo to the admin header.

Purpose: Make the user-facing directory premium and demo-ready with real brand logos, and ensure admin view shows tenant identity.
Output: Updated directory page and admin header.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-view-visual-polish/12-CONTEXT.md
@.planning/phases/12-user-view-visual-polish/12-01-SUMMARY.md
@src/app/page.tsx
@src/components/admin/AdminPage.tsx
@src/lib/tenants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand logos to directory cards in 2x3 grid</name>
  <files>src/app/page.tsx</files>
  <action>
Modify `src/app/page.tsx` to show brand logos in the existing card design and ensure the grid works as 2x3.

**Grid:** The existing grid classes `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` already produce 2x3 at lg breakpoint. Keep this — no change needed to the grid itself. With 6 tenants it will naturally fill 2 rows of 3.

**Card logo replacement:** In each card, replace the generic Bot icon placeholder with the tenant's actual brand logo:
- Replace the `<div className="p-2 bg-gray-100 rounded-lg ..."><Bot ... /></div>` block with an `<img>` tag rendering `tenant.theme.logoUrl`.
- Use: `<img src={tenant.theme.logoUrl} alt={tenant.displayName} className="w-10 h-10 object-contain rounded-lg" />` (keep the same ~40px sizing as the current icon container).
- If `logoUrl` is falsy, fall back to the existing Bot icon (defensive).
- Add `/* eslint-disable @next/next/no-img-element */` at the top of the file since we're using `<img>` for dynamic logo paths.

**Long name handling:** The `CardTitle` already uses `text-base`. Add `truncate` class to `CardTitle` to prevent text wrapping for long names like "SegurCaixa Adeslas":
- Change `<CardTitle className="text-base">` to `<CardTitle className="text-base truncate">`.

**Card style:** Do NOT change the Card component, hover effects, ChevronRight, or any other card styling. The user explicitly said "las tarjetas las quiero igual que ahora."

**Max width:** Update `max-w-3xl` to `max-w-4xl` on the container to give the 2x3 grid more breathing room with 6 cards.
  </action>
  <verify>
    <automated>cd "C:/Users/daniv/Proyectos/RAG LM" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit localhost:3000 — verify 6 cards in 2x3 grid, each showing brand logo, no text wrapping</manual>
  </verify>
  <done>Directory page renders 6 tenant cards in 2x3 grid. Each card shows the tenant's brand logo instead of the generic Bot icon. Long names like "SegurCaixa Adeslas" are truncated, not wrapped.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenant logo to admin header</name>
  <files>src/components/admin/AdminPage.tsx</files>
  <action>
Modify `src/components/admin/AdminPage.tsx` to display the active tenant's logo in the admin header.

**Step 1:** Import `getTenant` from `@/lib/tenants` at the top of the file (it's already imported in the server route, but AdminPage is a client component that receives `tenantId` — we need to look up the tenant object).

**Step 2:** Inside the `AdminPage` component body, resolve the tenant object:
```tsx
const tenantObj = getTenant(tenantId);
```

**Step 3:** In the admin header (the `<header>` element inside the authed return), add the tenant logo next to the title:
- After the `<Home>` link and before `<h1>`, insert:
```tsx
{tenantObj?.theme.logoUrl && (
  <img src={tenantObj.theme.logoUrl} alt={tenantObj.displayName} className="w-8 h-8 object-contain" />
)}
```
- Update the `<h1>` text from "Toolbox Admin" to show the tenant name: `{tenantObj?.displayName ?? 'Toolbox'} Admin`

**Step 4:** Add `/* eslint-disable @next/next/no-img-element */` at the top of the file.

Do NOT change anything else in AdminPage — no styling changes, no layout changes, no auth flow changes.
  </action>
  <verify>
    <automated>cd "C:/Users/daniv/Proyectos/RAG LM" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit localhost:3000/admin/calzedonia — verify header shows Calzedonia logo + "Calzedonia Admin" title</manual>
  </verify>
  <done>Admin header displays the active tenant's brand logo and tenant-specific title (e.g., "Calzedonia Admin") for all 6 tenants.</done>
</task>

</tasks>

<verification>
- Directory page: 6 cards visible in 2x3 grid at desktop width
- Each card: brand logo image, displayName (truncated if long), assistant name, chevron
- Admin header: logo + tenant name visible for any tenant URL
- TypeScript compiles: `npx tsc --noEmit` passes
- No card redesign — same Card/CardHeader/CardTitle structure
</verification>

<success_criteria>
Directory displays 6 branded tenant cards in a clean 2x3 grid with real logos. Admin header identifies the active tenant with logo and name. All user-facing views feel polished and demo-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-view-visual-polish/12-02-SUMMARY.md`
</output>
