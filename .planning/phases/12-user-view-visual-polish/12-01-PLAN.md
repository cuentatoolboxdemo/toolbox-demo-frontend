---
phase: 12-user-view-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tenants.ts
autonomous: true
requirements: [VIS-02, VIS-03]

must_haves:
  truths:
    - "TENANTS map contains exactly 6 entries: ide_marketing, calzedonia, sabor_a_espana, segurcaixa_adeslas, nex, rci"
    - "Former 'sabores' tenant is renamed to 'sabor_a_espana' with displayName 'Sabor a España'"
    - "Each tenant has a complete TenantTheme with brand-specific primary, primaryText, avatarGlow, avatarPulse, ring, and logoUrl"
    - "All logoUrl paths reference existing files in public/icons/"
  artifacts:
    - path: "src/lib/tenants.ts"
      provides: "6-tenant configuration with full themes and logo URLs"
      contains: "segurcaixa_adeslas"
  key_links:
    - from: "src/lib/tenants.ts"
      to: "public/icons/*.png"
      via: "logoUrl property in TenantTheme"
      pattern: "logoUrl.*public/icons"
---

<objective>
Expand the tenant roster from 3 to 6 branded tenants with full theme configuration and logo references.

Purpose: All downstream views (directory, chat, admin) read from TENANTS — this plan establishes the data foundation for the entire phase.
Output: Updated `src/lib/tenants.ts` with 6 fully-configured tenants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-view-visual-polish/12-CONTEXT.md
@src/lib/tenants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand TENANTS map to 6 branded tenants</name>
  <files>src/lib/tenants.ts</files>
  <action>
Modify `src/lib/tenants.ts` to contain exactly 6 tenants in the following order (this order matters for directory display):

1. **ide_marketing** (existing — UPDATE colors and add logoUrl):
   - displayName: "IDE Marketing"
   - name: "IDE Marketing Assistant"
   - primary: `#C00B00`, primaryText: `#FFFFFF`
   - avatarGlow: `#FF1A0D` (lighter red for glow), avatarPulse: `#8B0000` (dark red), ring: `#FF6B6B` (soft red)
   - logoUrl: `/icons/IDE Marketing_idNTrdiV4p_1.png`

2. **calzedonia** (existing — UPDATE colors and add logoUrl):
   - displayName: "Calzedonia"
   - name: "Calzedonia AI Assistant"
   - primary: `#8F2C4B`, primaryText: `#FFFFFF`
   - avatarGlow: `#B33D62` (lighter camelot), avatarPulse: `#6B1E36` (darker), ring: `#D4708F` (soft pink)
   - logoUrl: `/icons/Calzedonia_idFVQbCbFh_1.png`

3. **sabor_a_espana** (RENAME from `sabores` — change slug, displayName, name, UPDATE colors, add logoUrl):
   - slug: `sabor_a_espana`
   - displayName: "Sabor a España"
   - name: "Sabor a España Assistant"
   - primary: `#FF9C00`, primaryText: `#FFFFFF`
   - avatarGlow: `#FFB733` (lighter orange), avatarPulse: `#D98200` (darker), ring: `#FFD080` (soft gold)
   - logoUrl: `/icons/sabor1.png`

4. **segurcaixa_adeslas** (NEW):
   - slug: `segurcaixa_adeslas`
   - displayName: "SegurCaixa Adeslas"
   - name: "SegurCaixa Adeslas Assistant"
   - primary: `#0F77AE`, primaryText: `#FFFFFF`
   - avatarGlow: `#1A9AD9` (lighter denim), avatarPulse: `#0A5A82` (darker), ring: `#6BB8D9` (soft blue)
   - logoUrl: `/icons/segurcaixa.png`

5. **nex** (NEW):
   - slug: `nex`
   - displayName: "NEX"
   - name: "NEX Assistant"
   - primary: `#39FF14`, primaryText: `#FFFFFF`
   - avatarGlow: `#66FF44` (lighter neon), avatarPulse: `#2ACC10` (darker), ring: `#80FF66` (soft green)
   - logoUrl: `/icons/nex.png`

6. **rci** (NEW):
   - slug: `rci`
   - displayName: "RCI"
   - name: "RCI Assistant"
   - primary: `#F47216`, primaryText: `#FFFFFF`
   - avatarGlow: `#FF8C3A` (lighter orange), avatarPulse: `#C95C10` (darker), ring: `#FFB380` (soft peach)
   - logoUrl: `/icons/rci.png`

IMPORTANT:
- Remove the old `sabores` key entirely. Replace it with `sabor_a_espana`.
- Preserve the existing `TenantTheme` and `Tenant` types unchanged.
- Preserve `getTenant()` function unchanged.
- The TENANTS object keys must be in the order listed above (IDE Marketing first, RCI last).
  </action>
  <verify>
    <automated>cd "C:/Users/daniv/Proyectos/RAG LM" && npx tsx -e "const { TENANTS, getTenant } = require('./src/lib/tenants'); const keys = Object.keys(TENANTS); console.log('Count:', keys.length); console.log('Keys:', keys.join(', ')); console.log('Has sabores:', 'sabores' in TENANTS); console.log('Has sabor_a_espana:', 'sabor_a_espana' in TENANTS); keys.forEach(k => { const t = TENANTS[k]; console.log(k, ':', t.theme.logoUrl ? 'has logo' : 'MISSING logo', t.theme.primary); });"</automated>
    <manual>Verify 6 tenants exist, no "sabores" key, all have logoUrl and brand colors</manual>
  </verify>
  <done>TENANTS map has exactly 6 entries (ide_marketing, calzedonia, sabor_a_espana, segurcaixa_adeslas, nex, rci), each with complete TenantTheme including logoUrl pointing to existing asset files. Old "sabores" key is gone.</done>
</task>

</tasks>

<verification>
- `Object.keys(TENANTS).length === 6`
- No tenant has undefined/null theme properties
- All logoUrl paths correspond to existing files in `public/icons/`
- `getTenant('sabores')` returns null
- `getTenant('sabor_a_espana')` returns valid tenant
- TypeScript compiles without errors: `npx tsc --noEmit`
</verification>

<success_criteria>
Six tenants fully configured with brand-accurate colors and logo references. No references to old "sabores" slug remain in tenants.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-view-visual-polish/12-01-SUMMARY.md`
</output>
