---
phase: 12-user-view-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tenants.ts
  - public/logos/calzedonia.png
  - public/logos/sabores.png
  - public/logos/ide_marketing.png
  - public/logos/segurcaixa_adeslas.png
  - public/logos/rci_renault.png
  - public/logos/nex_michelin.png
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "6 tenants are defined in TENANTS map with unique slugs, display names, and brand themes"
    - "Each tenant has a logoUrl pointing to an existing file in /public/logos/"
    - "Brand colors for all 6 tenants reflect their real corporate identity"
  artifacts:
    - path: "src/lib/tenants.ts"
      provides: "6 tenant configurations with TenantTheme and logoUrl"
      contains: "segurcaixa_adeslas"
    - path: "public/logos/calzedonia.png"
      provides: "Calzedonia brand logo"
    - path: "public/logos/sabores.png"
      provides: "Sabor a Espana brand logo"
    - path: "public/logos/ide_marketing.png"
      provides: "IDE Marketing brand logo"
    - path: "public/logos/segurcaixa_adeslas.png"
      provides: "SegurCaixa Adeslas brand logo"
    - path: "public/logos/rci_renault.png"
      provides: "RCI Renault brand logo"
    - path: "public/logos/nex_michelin.png"
      provides: "NEX Michelin brand logo"
  key_links:
    - from: "src/lib/tenants.ts"
      to: "public/logos/*"
      via: "logoUrl field referencing /logos/{slug}.png"
      pattern: "logoUrl.*logos/"
---

<objective>
Expand the tenant registry to 6 branded tenants with real logos and accurate brand colors.

Purpose: All downstream plans (directory, avatar, admin) depend on having 6 fully-configured tenants with logo assets and brand palettes. This is the foundation layer.

Output: Updated tenants.ts with 6 entries, 6 logo image files in /public/logos/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-view-visual-polish/12-CONTEXT.md
@src/lib/tenants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source and save 6 brand logos</name>
  <files>
    public/logos/calzedonia.png
    public/logos/sabores.png
    public/logos/ide_marketing.png
    public/logos/segurcaixa_adeslas.png
    public/logos/rci_renault.png
    public/logos/nex_michelin.png
  </files>
  <action>
    Create the `/public/logos/` directory.

    For each of the 6 brands, source a high-quality logo from the web (official sites, Wikipedia, brand asset pages). Use `curl` or `node` fetch to download them. Target clean logos on transparent or white backgrounds, suitable for display at ~80px in cards and ~40px in avatar centers.

    The 6 brands and sourcing hints:
    1. **Calzedonia** — Italian legwear brand. Look for the wordmark logo (clean text). Source from official site or Wikipedia.
    2. **Sabor a Espana** (Sabores) — This is a fictional/internal brand. Generate a simple logo using a Node.js script with sharp or canvas: a warm orange circle with "S" letter, or download a suitable food/Spain themed icon. Keep it simple and branded.
    3. **IDE Marketing** — Internal marketing agency brand. Generate a clean logo: blue circle with "IDE" text, or use a marketing-themed icon. Use a Node.js script with sharp.
    4. **SegurCaixa Adeslas** — Spanish health insurance (AXA group). Source from official site or brand resources.
    5. **RCI (grupo Renault)** — Renault group financial services (RCI Banque / Mobilize Financial Services). Source Renault-group branding.
    6. **NEX (grupo Michelin)** — Michelin group tire tech. Source Michelin-related branding or generate a clean "NEX" logo.

    For brands where downloading a real logo fails or has CORS/access issues, generate a clean branded placeholder using a Node.js script with sharp: colored circle/rectangle with brand initials in contrasting text, sized 200x200px, saved as PNG.

    All logos must be saved as PNG, reasonable size (under 100KB each), recognizable at small sizes.
  </action>
  <verify>
    <automated>ls -la public/logos/ && node -e "const fs=require('fs'); const logos=['calzedonia','sabores','ide_marketing','segurcaixa_adeslas','rci_renault','nex_michelin']; const missing=logos.filter(l=>!fs.existsSync('public/logos/'+l+'.png')); if(missing.length) {console.error('MISSING:',missing);process.exit(1)} else console.log('All 6 logos present')"</automated>
  </verify>
  <done>6 PNG logo files exist in /public/logos/, each recognizable and under 100KB</done>
</task>

<task type="auto">
  <name>Task 2: Expand tenants.ts to 6 branded tenants</name>
  <files>src/lib/tenants.ts</files>
  <action>
    Update `src/lib/tenants.ts` to contain 6 tenant entries. Keep the existing 3 (calzedonia, sabores, ide_marketing) and add 3 new ones. Update ALL entries with `logoUrl` pointing to `/logos/{slug}.png`.

    The tenant order in the TENANTS map should be: ide_marketing, sabores, calzedonia, segurcaixa_adeslas, rci_renault, nex_michelin (this is the display order for the directory).

    Brand colors to research and apply (derive from official brand guidelines):
    - **Calzedonia**: Keep current black/white/gray theme (already accurate for their minimal aesthetic)
    - **Sabores**: Keep current orange theme (warm, food-related)
    - **IDE Marketing**: Keep current blue/indigo theme
    - **SegurCaixa Adeslas**: Use their brand red/dark blue. Primary: #c8102e (SegurCaixa red), avatarGlow: #e11d48, ring: #fca5a5
    - **RCI (grupo Renault)**: Use Renault's yellow/black. Primary: #f5c518 (Renault yellow), primaryText: #000000, avatarGlow: #eab308, ring: #fde68a
    - **NEX (grupo Michelin)**: Use Michelin's blue. Primary: #003d79 (Michelin blue), avatarGlow: #2563eb, ring: #93c5fd

    Each new tenant needs:
    - `slug`: the underscore-separated identifier
    - `name`: "{DisplayName} AI Assistant"
    - `displayName`: human-readable brand name
    - `theme`: full TenantTheme with primary, primaryText, avatarGlow, avatarPulse, ring, logoUrl

    Keep the TenantTheme type and Tenant type unchanged. Keep getTenant function unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && node -e "const t=require('./src/lib/tenants'); const slugs=Object.keys(t.TENANTS); if(slugs.length!==6){console.error('Expected 6 tenants, got',slugs.length);process.exit(1)} const need=['calzedonia','sabores','ide_marketing','segurcaixa_adeslas','rci_renault','nex_michelin']; const miss=need.filter(s=>!t.TENANTS[s]); if(miss.length){console.error('Missing:',miss);process.exit(1)} const noLogo=need.filter(s=>!t.TENANTS[s].theme.logoUrl); if(noLogo.length){console.error('No logoUrl:',noLogo);process.exit(1)} console.log('All 6 tenants with logos OK')"</automated>
  </verify>
  <done>tenants.ts exports 6 tenants, each with complete TenantTheme including logoUrl, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- 6 logo files exist in public/logos/
- 6 tenants in TENANTS map, each with logoUrl
- `npm run build` succeeds (no broken imports)
</verification>

<success_criteria>
All 6 tenants are fully configured with brand identity (colors + logos). Downstream plans can reference any tenant's theme.logoUrl and get a valid image path.
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-view-visual-polish/12-01-SUMMARY.md`
</output>
