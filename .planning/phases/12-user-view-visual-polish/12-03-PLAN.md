---
phase: 12-user-view-visual-polish
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/page.tsx
  - src/components/admin/AdminPage.tsx
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-06

must_haves:
  truths:
    - "Directory page displays 6 tenant cards in a 2x3 grid (2 rows, 3 columns on desktop)"
    - "Each directory card shows the tenant's real brand logo prominently"
    - "Cards use brand colors for hover effects and accents"
    - "Admin header displays the tenant logo next to the title with brand coloring"
    - "The directory page feels premium and polished as a first impression for demo clients"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Premium 2x3 branded tenant directory"
      min_lines: 40
    - path: "src/components/admin/AdminPage.tsx"
      provides: "Admin header with tenant logo and brand colors"
      contains: "logoUrl"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/tenants.ts"
      via: "TENANTS import for directory listing"
      pattern: "import.*TENANTS"
    - from: "src/app/page.tsx"
      to: "public/logos/*"
      via: "img src referencing logoUrl from tenant theme"
      pattern: "logoUrl|/logos/"
    - from: "src/components/admin/AdminPage.tsx"
      to: "src/lib/tenants.ts"
      via: "getTenant import for logo and brand color lookup"
      pattern: "getTenant|theme"
---

<objective>
Redesign the directory page as a premium 2x3 branded grid and add tenant logo branding to the admin header.

Purpose: The directory is the first thing demo clients see. It must feel premium, polished, and showcase real brand identities. The admin header should also reflect the active tenant's brand. User said "first impressions matter enormously."

Output: Redesigned page.tsx with branded 2x3 grid, updated AdminPage.tsx with logo in header.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-view-visual-polish/12-CONTEXT.md
@.planning/phases/12-user-view-visual-polish/12-01-SUMMARY.md
@src/app/page.tsx
@src/components/admin/AdminPage.tsx
@src/lib/tenants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign directory page as premium 2x3 branded grid</name>
  <files>src/app/page.tsx</files>
  <action>
    Rewrite the directory page (`src/app/page.tsx`) to display a premium 2x3 grid of branded tenant cards.

    **Layout:**
    - Keep `"use client"` and existing imports (useState, TENANTS, Tenant, LoginDialog)
    - Remove the `Bot` and `ChevronRight` lucide imports (no longer needed)
    - Add `/* eslint-disable @next/next/no-img-element */` for logo img tags
    - Grid: `grid-cols-2 sm:grid-cols-3` to get 2x3 on desktop (3 columns, 2 rows for 6 items), stack to 2 columns on small screens, 1 column on very small
    - Cards should be taller than current (to showcase logos prominently)

    **Card design (each tenant card):**
    - White card with rounded corners (rounded-2xl), subtle shadow, generous padding
    - Top section: Large brand logo (img, ~80px height, object-contain, centered)
    - Below logo: Tenant displayName in bold, centered
    - Below name: Tenant assistant name (tenant.name) in muted smaller text, centered
    - Bottom accent: A thin colored bar or gradient at the bottom of the card using tenant's theme.primary
    - Hover: Elevate shadow (shadow-lg), slight scale-up (scale-[1.02]), border color transitions to theme.primary
    - Use inline `style` for brand-color-dependent properties (border, accent bar, hover effects)
    - Smooth transitions on all hover effects (duration-200)
    - onClick triggers setSelectedTenant (same as current)

    **Page wrapper:**
    - Dark or very subtle gradient background (e.g., `bg-gradient-to-br from-gray-50 to-gray-100`) for premium feel
    - Centered content with max-width
    - "Toolbox AI" title: larger, bolder, maybe with a subtle gradient text or just clean black
    - Subtitle: "Selecciona un asistente para comenzar" (keep in Spanish)
    - More vertical spacing between title and grid

    **Keep existing LoginDialog integration** (selectedTenant state, onClose handler) -- no changes to auth flow.

    **Important:** The tenant order in the grid follows the order of Object.values(TENANTS), which Plan 01 set to: IDE Marketing, Sabores, Calzedonia, SegurCaixa Adeslas, RCI, NEX.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && node -e "const fs=require('fs'); const c=fs.readFileSync('src/app/page.tsx','utf8'); const checks=['grid-cols','logoUrl','displayName','LoginDialog','setSelectedTenant']; const missing=checks.filter(k=>!c.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('Directory page OK')"</automated>
    <manual>Open root page. Verify 2x3 grid with 6 branded cards, real logos, brand-colored hover effects. Check mobile responsiveness.</manual>
  </verify>
  <done>Directory page shows 6 tenant cards in a 2x3 grid, each with real brand logo, display name, and brand-colored accents. Page feels premium and polished.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenant logo and brand color to admin header</name>
  <files>src/components/admin/AdminPage.tsx</files>
  <action>
    Update `AdminPage.tsx` to display the active tenant's logo and brand color in the header.

    **Changes to admin header (authenticated view):**
    - Import `getTenant` from `@/lib/tenants`
    - Add `/* eslint-disable @next/next/no-img-element */` at top
    - In the header, look up the tenant: `const tenant = getTenant(tenantId)`
    - Before the "Toolbox Admin" title, add the tenant logo: `<img src={tenant?.theme.logoUrl} alt={tenant?.displayName} className="w-8 h-8 object-contain" />`
    - Change the header title to include tenant name: "Toolbox Admin - {tenant?.displayName}" or just show tenant displayName next to the generic title
    - Add a thin top border or accent line using tenant's theme.primary color (e.g., `borderTop: 3px solid ${tenant?.theme.primary}`)
    - Keep existing header layout (flex row, justify-between, Home link, Logout button)

    **Login view:** No changes needed (it's generic across tenants).

    **Keep all existing functionality intact:** doc list, upload zone, system prompt editor, login/logout flow, delete handler.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/admin/AdminPage.tsx','utf8'); if(!c.includes('getTenant')){console.error('Missing getTenant import');process.exit(1)} if(!c.includes('logoUrl')||!c.includes('logo')){console.error('Missing logo integration');process.exit(1)} console.log('Admin header OK')"</automated>
    <manual>Open admin for any tenant. Verify logo appears in header, brand color accent visible.</manual>
  </verify>
  <done>Admin header shows tenant logo and brand-colored accent. All existing admin functionality unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all views</name>
  <what-built>
    Premium 2x3 branded directory page with real logos and brand colors, plus admin header with tenant logo and brand accent.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/ — verify 6 tenant cards in 2x3 grid, each showing real brand logo, display name, and brand-colored accents on hover
    2. Click any tenant card — verify login dialog appears
    3. Log in as admin to any tenant — verify admin header shows tenant logo and brand color
    4. Check mobile viewport (resize browser to ~375px width) — verify cards stack responsively
    5. Compare different tenants — verify each has distinct brand identity (colors, logos)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Directory shows 2x3 grid with 6 branded tenants
- Admin header shows tenant logo and brand accent
- All existing auth and admin functionality intact
</verification>

<success_criteria>
The directory page makes an excellent first impression with premium branded cards. Each of the 6 tenants is visually distinct with real logos and brand colors. The admin header reflects the active tenant's brand identity. The overall experience feels demo-ready and polished.
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-view-visual-polish/12-03-SUMMARY.md`
</output>
