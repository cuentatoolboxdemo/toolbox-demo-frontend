---
phase: 12-user-view-visual-polish
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified: []
autonomous: false
requirements: [VIS-04, VIS-05, VIS-06]

must_haves:
  truths:
    - "All 6 tenants render correctly in the directory with brand logos and colors"
    - "Chat avatar displays tenant logo in center and uses tenant brand colors for glow/rings (existing AnimatedAvatar behavior per user decision — no particle constellation)"
    - "Avatar reacts to state: gentle animation idle, accelerated animation when thinking (existing behavior preserved)"
    - "Overall user-facing experience looks premium and demo-ready"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all user-facing views across the 6 branded tenants.

Purpose: Confirm the full visual polish chain works end-to-end — directory, chat, and admin views all reflect correct branding for every tenant.
Output: Human-verified visual quality across all tenant views.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-view-visual-polish/12-CONTEXT.md
@.planning/phases/12-user-view-visual-polish/12-01-SUMMARY.md
@.planning/phases/12-user-view-visual-polish/12-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all user-facing views across 6 tenants</name>
  <files>src/app/page.tsx, src/lib/tenants.ts, src/components/admin/AdminPage.tsx</files>
  <action>
Human visual verification of the full visual polish chain.

What was built:
- Directory page with 2x3 grid showing brand logos for 6 tenants
- Chat view with tenant-specific avatar colors and center logo
- Admin header with tenant logo and name

Note on VIS-04/VIS-05 (particle constellation): Per user decision in CONTEXT.md, the avatar was NOT changed. The existing AnimatedAvatar already provides spinning rings, decorational dots, and state-reactive behavior (idle vs thinking). The user explicitly said "NO" to changing the avatar. These requirements are satisfied by the existing implementation.

How to verify:
1. Start dev server: `npm run dev`
2. Visit `http://localhost:3000` — verify:
   - 6 cards in 2x3 grid (desktop)
   - Each card shows correct brand logo (not generic Bot icon)
   - "SegurCaixa Adeslas" name does NOT wrap to 2 lines
   - Cards maintain existing style (hover shadow, chevron, etc.)

3. Click each tenant to enter chat — verify for each:
   - Avatar center shows tenant's brand logo
   - Avatar glow/ring colors match brand
   - Avatar animates gently on idle
   - Send a test message — avatar should react (pulse/spin acceleration)
   - User message bubble uses tenant's primary color

4. Visit admin for 2-3 tenants (e.g. `/admin/calzedonia`, `/admin/nex`, `/admin/rci`) — verify:
   - Header shows tenant logo + "[TenantName] Admin" title
   - Rest of admin page unchanged

5. Overall impression: Does it feel premium and demo-ready?
  </action>
  <verify>
    <manual>User visually inspects directory (6 cards, logos, 2x3 grid), chat (avatar colors/logo per tenant), and admin (header logo) for all 6 tenants. Type "approved" or describe issues.</manual>
  </verify>
  <done>User confirms all 6 tenants display correctly across directory, chat, and admin views. Visual quality is premium and demo-ready.</done>
</task>

</tasks>

<verification>
Human visual verification of all 6 tenants across directory, chat, and admin views.
</verification>

<success_criteria>
User confirms all views are visually polished, branded correctly, and demo-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-view-visual-polish/12-03-SUMMARY.md`
</output>
